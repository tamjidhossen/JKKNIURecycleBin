package com.example.recyclebin;

import com.example.recyclebin.models.ModelCategoryHome;

//useless, but don't remove yet
public interface RvListenerCategory {
    void onCategoryClick(ModelCategoryHome modelCategoryHome);
}
